---
name: bocha-search
description: 使用博查AI搜索获取实时网络信息，支持中文搜索和长文本摘要
metadata:
  openclaw:
    emoji: 🔍
    skillKey: bocha-search
    requires:
      env:
        - BOCHA_API_KEY
      config:
        - plugins.entries.bocha-search.enabled
---

# 博查搜索

使用博查AI搜索工具获取实时网络信息，特别适合中文搜索场景。

## 何时使用

当用户需要：
- 搜索中文网页内容
- 查找最新的新闻资讯
- 获取某个话题的详细摘要
- 需要比内置 web_search 更好的中文搜索结果

## 使用方法

使用 `bocha_web_search` tool：

```
bocha_web_search
query: 搜索关键词
freshness: oneWeek (可选，时间范围)
count: 5 (可选，结果数量)
summary: true (可选，是否返回长摘要)
```

## 参数说明

- **query** (必填): 搜索关键词，支持自然语言
- **freshness** (可选): 时间范围
  - `oneDay` - 一天内
  - `oneWeek` - 一周内
  - `oneMonth` - 一月内
  - `oneYear` - 一年内
  - `noLimit` - 不限（默认）
- **count** (可选): 返回结果数量，1-10，默认 5
- **summary** (可选): 是否返回长文本摘要，默认 true

## 示例

**搜索最新新闻：**
```
bocha_web_search
query: OpenAI 最新发布
freshness: oneWeek
count: 5
```

**搜索技术文档：**
```
bocha_web_search
query: React 19 新特性
summary: true
count: 8
```

**搜索产品信息：**
```
bocha_web_search
query: iPhone 16 评测
freshness: oneMonth
count: 10
```

## 注意事项

- 博查搜索对中文内容支持较好
- 长摘要(summary=true)会返回更详细的内容，但响应稍慢
- 如遇到搜索失败，检查 API Key 是否配置正确
